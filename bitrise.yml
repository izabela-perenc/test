format_version: 9

workflows:
  test:
    steps:
    - git::https://github.com/izabela-perenc/bitrise-step-upload-to-appsweep.git@master:
        title: AppSweep
        inputs:
        - debug: true
        - protected: true
