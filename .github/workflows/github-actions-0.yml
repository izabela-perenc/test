name: GitHub Action AppSweep
on: [push]
jobs:
  AppSweep-action:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - run: ./gradlew uploadToAppSweepRelease --stacktrace
